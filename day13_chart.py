import streamlit as st
import pandas as pd

st.title("📈 2026년 콩 매출 분석")
st.write("우리 농장의 일주일간 매출 추이를 한눈에 확인하세요.")

# 1. 엑셀 데이터 만들기 (DataFrame)
# 날짜와 매출액을 딕셔너리 형태로 만듭니다.
data = {
    '날짜': ['1월 1일', '1월 2일', '1월 3일', '1월 4일', '1월 5일', '1월 6일', '1월 7일'],
    '매출(원)': [50000, 42000, 65000, 38000, 72000, 85000, 90000]
}

# pd.DataFrame -> 파이썬 안에 엑셀 시트를 만드는 명령어
df = pd.DataFrame(data)

# 2. 화면 나누기 (왼쪽: 표, 오른쪽: 그래프)
col1, col2 = st.columns([1, 2]) # 1:2 비율로 나누기

with col1:
    st.subheader("📋 매출 장부")
    st.dataframe(df) # 엑셀 표처럼 보여줌

with col2:
    st.subheader("🚀 매출 성장 그래프")
    # 날짜를 가로축(X축)으로 쓰기 위해 설정
    chart_data = df.set_index('날짜')
    st.line_chart(chart_data) # 선 그래프 그리기