# streamlit 사이드바(sidear)에 "메뉴 선택" 박스 만들기
# 옵션은 ["홈", "날씨 조회", "매출 분석"] 으로 해줘
# 사용자가 선택한 메뉴에 따라 메인 화면에 다른 제목과 내용을 보여줘.
import streamlit as st
import pandas as pd
import random

# 1. 사이드바 꾸미기 (왼쪽 서랍)
st.sidebar.title("🌾 농장 관리 시스템")
st.sidebar.write("로그인: 홍길동 농부님")

# 핵심: 사이드바에 메뉴 만들기 (selectbox)
menu = st.sidebar.selectbox(
    "원하는 메뉴를 선택하세요:",
    ["홈 (Home)", "날씨 조회 (Weather)", "매출 분석 (Sales)"]
)

st.sidebar.success("메뉴를 선택하면 화면이 바뀝니다.")

# 2. 선택된 메뉴에 따라 다른 화면 보여주기
st.title("🌱 제주 푸른 콩 농장")

if menu == "홈 (Home)":
    st.header("환영합니다!")
    st.image("https://images.unsplash.com/photo-1550989460-0adf9ea622e2", caption="우리 농장 전경")
    st.write("오늘도 안전한 농작업 되세요.")

elif menu == "날씨 조회 (Weather)":
    st.header("☁️ 실시간 기상 관제")
    # (복습) 날씨 기능 흉내내기
    temp = random.randint(10, 25)
    st.metric(label="현재 기온", value=f"{temp}°C", delta="-2°C")
    st.info("비가 올 확률이 30%입니다. 배수로를 점검하세요.")

elif menu == "매출 분석 (Sales)":
    st.header("💰 이번 주 매출 현황")
    # (복습) 그래프 기능 흉내내기
    data = {'날짜': ['월', '화', '수', '목', '금'], '매출': [30, 45, 60, 40, 80]}
    df = pd.DataFrame(data).set_index('날짜')
    st.bar_chart(df)